apiVersion: 1

providers:
  - name: "ErrandBridge"
    orgId: 1
    folder: "ErrandBridge"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
